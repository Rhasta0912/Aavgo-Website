name: Deploy Website

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Prepare deploy bundle
        run: |
          mkdir -p deploy

          cp index.html deploy/index.html
          cp styles.css deploy/styles.css
          cp script.js deploy/script.js
          cp -R admin deploy/admin
          cp -R user deploy/user
          cp -R api deploy/api
          cp -R auth deploy/auth
          printf "sha=%s\nbuilt_at=%s\n" "$GITHUB_SHA" "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > deploy/_aavgo_deploy.txt

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Upload site to cPanel
        env:
          CPANEL_FTP_HOST: web-slc-02.sparkedhost.us
          CPANEL_FTP_USERNAME: Web@aavgodesk.xyz
          CPANEL_FTP_PASSWORD: ${{ secrets.CPANEL_FTP_PASSWORD }}
          CPANEL_FTP_PORT: 21
        run: |
          test -n "$CPANEL_FTP_HOST"
          test -n "$CPANEL_FTP_USERNAME"
          test -n "$CPANEL_FTP_PASSWORD"
          test -n "$CPANEL_FTP_PORT"

          lftp -u "$CPANEL_FTP_USERNAME","$CPANEL_FTP_PASSWORD" -p "$CPANEL_FTP_PORT" "$CPANEL_FTP_HOST" -e "
          set cmd:fail-exit yes
          set ftp:ssl-force true
          set ftp:ssl-protect-data true
          set ssl:verify-certificate no
          mirror -R --verbose deploy/ /
          mirror -R --verbose deploy/ public_html/
          bye
          "
